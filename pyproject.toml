[project]
name = "universcale"
license = "MIT"
license-files = ["LICENSE"]
version = "0.0.1"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
